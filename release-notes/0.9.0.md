# v0.9.0 Release Notes

**Release Date:** Q3 2026 (planned)
**Database Schema:** v20 (frontend IndexedDB) + v5 (backend PostgreSQL via Flyway, planned)
**Status:** In Progress

---

## Overview

v0.9.0 ships the full UX uplift across two waves: the first wave (three-voice typography, Almanac
ornaments, universal quick-add, diary-first dashboard, streamlined logging with null macros, and
photo logging UI scaffold) and the second wave (retention rituals, power-user navigation,
multi-select re-log, system polish, contextual onboarding, progress visualization, and platform
personalization including PWA, haptics, accent theming, and Settings IA), plus a quality pass via
simplification cleanup (9 deduplication + efficiency fixes). The Dexie v20 schema (photos table +
FoodPhotoId) and the 9th Zustand slice (uiSlice) ship with the first wave.

---

## UX Uplift - First Wave (Complete - June 7, 2026)

Six workstreams from `docs/canvas-design-philosophy.md`, implementing the
Phenological Ledger / Almanac design system across all surfaces.

**Test delta:** 90 test files / 1796 tests (v0.8.0) - 100 test files / 1900 tests (v0.9.0 first wave)
**Coverage:** statements 90.88%, functions 90.25%, branches 81.17%, lines 92.63%

### Three-voice typography (Complete)

- `Spectral` variable font added via `@font-face` in `style.css`; `--font-serif` token wired
- `SERIF_TITLE_CLS` constant exported from `lib/utils.ts`: `text-[1.35rem] font-serif font-light
  tracking-tight leading-snug text-[color:var(--ink)]`
- `SectionHeader.tsx` rewritten: serif heading + JetBrains Mono eyebrow + rule line replaces the
  previous generic bold heading; three-voice hierarchy (Spectral / Manrope / JetBrains Mono)
  activated throughout Dashboard, Recipes, Progress, and Settings
- `DateKicker.tsx` updated to JetBrains Mono micro-label style

**Architectural decision D27:** Spectral chosen over Fraunces (on the UX-principles reflex-reject
ban list) and Libre Baskerville (too neutral). Spectral's optical size range covers both `text-sm`
body use and oversized hero numerics without weight-clash against Manrope 200 thin.

### Almanac ornament activation (Complete)

- `RuleTicks.tsx` added to `components/icons/almanac/`: decorative SVG tick marks at rule
  intersections (uses `--rule` color token, `aria-hidden`)
- Paper-grain surface texture added to `style.css` as a CSS `background-image` data-URI noise
  pattern; applied via `.grain` utility class on the app shell; fallback: `background: transparent`
  for `prefers-reduced-transparency`
- All five Almanac SVG ornaments (`WheatSprig`, `MoonPhase`, `SunRay`, `RuleCorner`,
  `SeasonalFlourish`) wired into actual Dashboard and section divider positions (previously
  defined but unused)

### Universal quick-add (Complete)

- `QuickAddModal.tsx` built on native `<dialog>` element (not Radix Dialog): top-layer positioning,
  native light-dismiss via `::backdrop`, no portal hacks, native `aria-modal` semantics
- `uiSlice.ts` added as the 9th Zustand slice: `quickAddOpen: boolean`, `openQuickAdd()`,
  `closeQuickAdd()` actions; composed into `AppState.ts`
- `Cmd/Ctrl+K` global keyboard shortcut opens the modal via `useKeyboardShortcuts`
- `QuickAddFab.tsx`: center `+` FAB rendered in mobile bottom nav (`@media (pointer: coarse)`);
  `env(safe-area-inset-bottom)` padding for iPhone notch safety
- Removes all `document.querySelector` focus-hacks previously used to trigger log forms

**Architectural decision D28:** Native `<dialog>` chosen over Radix `Dialog`. The Radix primitive
adds ~4 kB to the vendor-ui chunk; native `<dialog>` gives the same top-layer guarantee (Chrome
37+, Firefox 98+, Safari 15.4+) with zero bundle cost and better `showModal()` stacking context.
All browsers in our support matrix support it.

### Diary-first dashboard (Complete)

- `Dashboard.tsx` restructured: calorie ring + macro stats remain above the fold; food log moves to
  primary position below; tracker cards (water, steps, activity, fasting) moved behind "Week" tab
- `DailyVitalsStrip.tsx` added to `components/dashboard/`: compact single-row display of the four
  tracker values with inline quick-log; expands to full tracker card on tap
- Today / Week tab strip added using roving `tabindex` and `aria-selected`; hash navigation not
  changed (still `#/`)
- All empty diary states now have inline CTA buttons ("Log your first meal") rather than passive
  placeholder text
- `EmptyState.tsx` `quiet` variant wired into the diary empty state

### Streamlined logging (Complete)

- `FoodSearchCombobox.tsx` added: Popover API + CSS anchor positioning (`lib/anchor.ts`);
  `fuzzyMatchFoodName()` from `types/index.ts` powers the suggestion list; `getRecentFoodItems()`
  IndexedDB query added to `dbService.ts` (uses `[userId+dateLogged]` compound index, most-recent
  5 entries); keyboard navigation via `aria-activedescendant`
- `useRecentFoods.ts` hook: wraps `getRecentFoodItems()` and exposes recent food results to
  `FoodSearchCombobox` as reactive state
- `FoodLogger.tsx` refactored for progressive disclosure: name + calories + meal type visible
  immediately; protein / carbs / fat / serving size behind "Add macros" expand toggle
- **Null macros:** `protein`, `carbs`, `fat` on `FoodItem` changed from `number` (required) to
  `number | undefined`; Zod schema updated (`z.number().optional()`); all render sites updated to
  show `"---"` when the value is `undefined` rather than `"0"`
- `MacroStat.tsx` updated: renders `"---"` with `text-[color:var(--ink-faint)]` when macro is
  unset; never shows `0` for genuinely unknown macros

**Architectural decision D29:** Null macros (`number | undefined`) chosen over keeping `0`
defaults. The `0` default silently corrupted macro totals (a food logged as 0g protein is
different from one with unknown protein). The `undefined` representation is propagated through
IndexedDB (field simply absent), Zustand state, and all render sites. The Zod schema uses
`.optional()` with no `.default(0)` fallback.

### Photo logging scaffold (Complete)

- `PhotoFoodLogger.tsx`: camera-capture UI with `<input type="file" capture="environment">`;
  generates a 200px-width JPEG thumbnail via `OffscreenCanvas`; stores base64 thumbnail to Dexie
  `photos` table; attaches `photoId` to the food log entry
- `ProvenanceBadge.tsx`: small inline chip showing how a food entry was logged (manual, voice,
  photo, barcode, recurring, template); uses `lucide-react` icons; `aria-label` on icon-only
  display
- `PhotoStrip.tsx`: horizontal scroll strip of meal photo thumbnails on the diary; lazy-loads
  from Dexie `photos` table; taps open a lightbox dialog
- **Dexie v20:** `photos` table added (`++id, userId, createdAt`); `FoodPhotoId` branded type
  added to `types/index.ts`; `photoId?: FoodPhotoId` added to `FoodItem` interface; migration
  from v19 is additive (no data loss)
- `FoodItem.photoId?: FoodPhotoId` wired into `FoodLogger` and `dbService.ts` CRUD methods

**Architectural decision D30:** Local Dexie v20 photos table chosen over S3 upload for the photo
logging scaffold. The AI photo logging backend (S3 + vision LLM) was not yet available; a local-first photo store unblocks the UI
scaffold and photo history UX immediately. Base64 thumbnails at 200px width average ~8 kB per
photo - acceptable for typical meal-log counts. Migration to remote URLs is a field-type change
in v21+.

---

## AI Photo Logging (Planned)

### Backend - Planned

**Photo upload + storage**

- Photo upload endpoint: `POST /api/v1/photos` - accepts multipart/form-data; stores to S3 (or
  local filesystem fallback for dev)
- Flyway migration `V5__photos.sql`: `photos` table with `id`, `user_id`, `s3_key`,
  `thumbnail_key`, `created_at`, `deleted_at`
- `PhotoService`: upload, presigned URL generation, soft-delete
- `PhotoController`: `POST /photos`, `GET /photos/{id}/url`, `DELETE /photos/{id}`

**Vision LLM proxy**

- `POST /api/v1/photos/{id}/analyze` - sends image to vision LLM (Claude claude-sonnet-4-6 or
  GPT-4V); returns structured nutrition estimate
- `AnalysisResult` DTO: `{ items: [{ name, calories, protein?, carbs?, fat?, servingSize,
  confidence }] }`
- Prompt engineering: few-shot examples for common meal types; confidence threshold gating
- `VisionAnalysisService`: wraps Anthropic/OpenAI SDK; timeout 30s; retries 2x on transient
  errors

**New env vars:**

- `S3_BUCKET`, `S3_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` (or
  `USE_LOCAL_STORAGE=true`)
- `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` (one required for analysis)
- `VISION_MODEL` (default: `claude-sonnet-4-6`)

### Frontend - Planned (AI wiring)

**Wire `PhotoFoodLogger.tsx` to real backend**

- Replace local Dexie thumbnail with real `POST /api/v1/photos` upload
- Show analysis results from `POST /photos/{id}/analyze` in editable confirmation UI
- Allow per-item calorie/macro adjustment before committing to food log
- Spinner + timeout handling (analysis can take 5-15s)

---

## UX Uplift - Second Wave (Complete - June 10, 2026)

Seven feature groups reusing primitives built in the first wave.

### Retention rituals (Complete - June 9, 2026)

**`motionVariants.ts`**

- Staggered page-load choreography with named crossfade variants
- `useSectionMotion()` and `useHeroSection()` consolidated for reduced duplication
- `heroVariants` (scale 0.98->1 + fade) and `counterPopVariants` (spring pop on number change,
  use with `key={value}`) added
- `easeSpring` token added for consistent spring easing across components

**`StreakCard.tsx`**

- Streak milestone display with motion variants
- Calendar-based day grid rendering

**`WeeklyHarvestModal.tsx`**

- Weekly review modal that fires once per new ISO week via `useWeeklyHarvestTrigger`
- Summarizes the week: streak count, calorie balance, and top logged foods
- `HarvestStamp` achievement-reveal animation included for streak milestones

**`InsightCard.tsx`**

- Personalized dashboard nudges ("You logged dinner 6 of the last 7 days")
- Dismissable; dismissed insight IDs stored in `dismissedInsights: string[]` (ephemeral
  `useState` in `Dashboard.tsx`)

**`useWeeklyHarvestTrigger.ts`**

- Fires `openWeeklyHarvest()` when the ISO week changes; tracks `lastHarvestWeek` in
  `localStorage` to fire exactly once per new week
- `useWeeklyHarvestTrigger.test.ts` added: covers week-change trigger and already-seen guard

### Power-user navigation (Complete - June 9, 2026)

**`DateKicker.tsx`**

- Past-date navigation rewritten: `shiftISODate()` helper eliminates duplicate date arithmetic
- Previous/Next day buttons with boundary checks
- "Go to today" button when viewing past dates

**`types/index.ts`**

- `shiftISODate(date: ISODate, n: number): ISODate` exported for date navigation across codebase

**`coreSlice.ts`**

- `selectedDate: ISODate` state added (defaults to `todayISO()`); enables viewing any past date
- `setSelectedDate(date: ISODate)` action: updates `selectedDate` and refetches food/water/step/activity logs for the new date
- `refreshDailyLogs()` updated to use `get().selectedDate` instead of hardcoded `todayISO()`

**`CommandPalette.tsx`**

- Cmd/Ctrl+K fuzzy-search palette across food logs, recipes, and settings navigation
- `commandPaletteOpen: boolean` + `openCommandPalette()` / `closeCommandPalette()` actions in
  `uiSlice.ts`; wired to the existing `Cmd/Ctrl+K` shortcut via `useKeyboardShortcuts`
- Keyboard navigation: arrow up/down moves selection, Enter executes, Escape dismisses
- `CommandPalette.test.tsx` added

### Logging speed (Complete - June 10, 2026)

**`foodSlice.ts`**

- `copyYesterdayLogs()` refactored to use `shiftISODate()` helper for cleaner date arithmetic

**`useSyncService.ts`**

- Optimized `flushQueue()`: eliminated redundant `syncQueue.count()` DB query by passing count via
  callback from already-fetched entries array

**`StepTracker.tsx`, `WaterTracker.tsx`**

- Tap-to-add quick-log buttons with loading state management

**`Dashboard.tsx`**

- Multi-select re-log: select one or more food entry chips to re-log them to a different meal
  type in one action
- `isMultiSelect: boolean` and `selectedIds: Set<string>` held in ephemeral `useState` (not
  Zustand - transient selection UI state); `multiSelectMealType: MealType` for the target meal
- `toggleChipSelection(key)`, `handleMultiLog()`, `cancelMultiSelect()` handlers
- `role="checkbox"` + `aria-checked` on chip `<button>` elements for accessibility; native
  `<select aria-label="Meal type">` for target meal type; Log button `disabled` when
  `selectedIds.size === 0`
- `Dashboard.test.tsx`: 9 multi-select tests (enter mode, select/deselect, log to different
  meal type, cancel, disabled state when no items selected)

### System polish (Complete - June 10, 2026)

**`SyncStatusChip.tsx`, `syncSlice.ts`**

- `pendingSyncCount: number` state added to `syncSlice.ts` with `setPendingSyncCount` action;
  `SyncStatusChip` now shows "Syncing (N)" when queue is non-empty during a sync
- `aria-hidden={true}` explicit boolean on all icon elements (replaces implicit `aria-hidden`
  without `={true}`)

**`uiSlice.ts`**

- `commandPaletteOpen: boolean` + `openCommandPalette()` / `closeCommandPalette()` actions added;
  infrastructure for the command palette feature
- `density: Density` state (`"comfortable" | "compact" | "spacious"`) + `setDensity()` action;
  persisted to `localStorage` as `gc_density`; defaults to `"comfortable"` on first load

**`Settings.tsx`**

- Display section added between Goals and Reminders: layout density toggle (comfortable / compact /
  spacious) built with `role="radiogroup"` and `aria-checked` on each radio button
- Section nav breadcrumb updated to include "Display": "Profile · Diet · Goals · Display ·
  Reminders · Data · About"

### Onboarding / education (Complete - June 8, 2026)

**`ContextualCoachmark.tsx`**

- New component: inline contextual tip card shown once per feature surface; dismissed via a
  "Got it" button that records the coachmark id as seen
- `markCoachmarkSeen(id: string)` action and `seenCoachmarks: string[]` state added to
  `uiSlice.ts`; persisted in localStorage alongside the existing density preference
- `CONTEXTUAL_COACHMARKS` map added to `tourSteps.ts`: 3 initial coachmark definitions
  (`food-logger`, `water-tracker`, `command-palette`) with title, body, and icon

**`OnboardingModal.tsx` / `useOnboarding.ts`**

- `ONBOARDING_TOTAL_STEPS` updated 4 -> 5; step 5 introduces the privacy notice
- Step copy updated: explicit "data never leaves this device" notice; Google sign-in framed as
  optional cloud backup, not a required step

**`OnboardingBanner.tsx`**

- Visual banner on Dashboard until onboarding is complete; links directly to the product tour;
  disappears once `onboardingComplete` is `true` in Zustand

**Test files added:**

- `ContextualCoachmark.test.tsx`: 5 tests (render, unknown id, already-seen, dismiss, body text)
- `OnboardingBanner.test.tsx` (TC4): covers CTA click and already-complete suppression

### Progress / nutrition viz (Complete - June 9, 2026)

**`chartTheme.ts`**

- `trend: "var(--ink-soft)"` semantic color token added to the chart palette; used for overlay
  and trend lines to distinguish them from the primary data series

**`EditorialChartCard.tsx`**

- `eyebrow?: string` prop added; renders as `font-mono text-xs` micro-label above the Spectral
  serif chart title, completing the three-voice hierarchy inside chart cards
- `label` style updated to `editorial-serif text-sm font-semibold text-ink` for visual
  consistency with section headings

**`ChartTooltip.tsx`**

- Hairline `h-px bg-rule` divider added between the date label and the payload rows; matches
  the `RuleTicks` + hairline rule language used across Dashboard

**`useProgressData.ts`**

- `rollingAvg: number[]` added to the return type, internal state, and computed output
- 7-day trailing average computed via a sliding-window reduce over `calorieTotals`: each entry
  is the mean of the current day and up to 6 previous days

**`Progress.tsx`**

- Calorie chart upgraded from `BarChart` to `ComposedChart`: `Area` for daily totals (filled,
  primary color) + `Line` for the rolling average (dashed, `chartTheme.trend` stroke)
- Weight trajectory `EditorialChartCard` added with `eyebrow` showing the current trend
  direction alongside the historical weight line
- Before/after body-measurement delta strip: first vs latest measurement for weight, waist, and
  hip; color is goal-aware (green when progressing toward goal, amber otherwise)
- `BodyScale` SVG illustration used as the Progress page empty state when no measurements exist

**Test files updated:**

- `useProgressData.test.ts`: 3 new tests (return shape, length parity with `calorieTotals`,
  7-day average math verified against known values)
- `EditorialChartCard.test.tsx`: 4 new tests (eyebrow renders, absent eyebrow renders cleanly,
  eyebrow uses mono class, label uses serif class)

**Pending (deferred to platform personalization / post-v0.9.0):**

- Forecast line: projected weight at current calorie deficit/surplus rate (30-day horizon)
- Micronutrient heatmap: 7-day calendar view of RDA % per micronutrient

### Platform / personalization (Complete - June 10, 2026)

**`vite.config.ts`**

- PWA manifest polished: `id`, `start_url`, `scope`, `orientation: "portrait"`, `lang: "en"`,
  `categories: ["health", "fitness"]`, and `screenshots` array for Play Store / App Store prompts
- `vite-plugin-pwa` + Workbox configured for offline-first caching; installable on iOS Safari and
  Android Chrome

**`index.html`**

- iOS PWA meta tags added: `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`,
  `apple-mobile-web-app-title`, and `apple-touch-icon` links for home screen appearance

**`lib/haptics.ts`**

- New module: typed `triggerHaptic(pattern: HapticPattern)` using `navigator.vibrate()`; checks
  `coarse` pointer and `prefers-reduced-motion` before firing
- Patterns: `"success"` (short buzz), `"achievement"` (double buzz), `"error"` (long buzz)
- `haptics.test.ts` added: 9 tests covering all patterns, coarse-pointer guard, reduced-motion
  guard

**`uiSlice.ts`**

- `hapticsEnabled: boolean` state + `setHapticsEnabled()` action; persisted to `localStorage` as
  `gc_haptics`; defaults to `true`
- `accentColor: AccentColor` state (`"persimmon" | "sage" | "slate" | "ocean" | "ember"`) +
  `setAccentColor()` action; persisted to `localStorage` as `gc_accent`; CSS custom property
  `--color-accent` updated on the `<html>` element on change

**`Settings.tsx`**

- Settings IA redesign: search bar at top filters all settings sections; sections grouped by
  category (Profile, Diet, Goals, Display, Reminders, Data, About) with visible group labels
- Display section expanded: haptics toggle + 5-color accent picker (persimmon / sage / slate /
  ocean / ember) alongside the density selector

---

## Social / Sharing (Planned)

- Share a day's food log or recipe as a card image (canvas-based)
- Follow other users; see their public recipes
- Challenge mode: shared streak goals with friends
- Backend: `friendships`, `shares` tables via Flyway `V6__social.sql`

---

## Test Debt Cleanup (TC1-TC4)

Four components identified in the June 2026 coverage audit that are missing paired test files:

| ID  | Component                      | Priority | What to test                                       |
|-----|--------------------------------|----------|----------------------------------------------------|
| TC1 | `ActivityTracker.tsx`          | High     | Zustand integration, calorie calc, empty state     |
| TC2 | `RecurringMeals.tsx`           | High     | Form mutations, IndexedDB interactions via hook    |
| TC3 | `KeyboardShortcutsOverlay.tsx` | Medium   | Renders shortcut list; correct key labels per OS   |
| TC4 | `OnboardingBanner.tsx`         | Medium   | Reads `onboardingComplete` from Zustand; CTA click |

Target: add `.test.tsx` for all 4 before v0.9.0 ships.

---

## Technical Debt / Cleanup

- `MOCK_USER_ID = UserId("1")` in `App.tsx`: requires multi-user architecture decision (H1 from
  OWASP audit) before cloud accounts can support multiple local profiles
- Refresh token in `localStorage` (`gc_refresh_token`): migrate to httpOnly cookie when backend
  supports it (D18 known tradeoff; deferred to v0.9.0 or later)
- Barcode lookup (Feature 12): scanner UI exists; nutrition API integration (USDA / Open Food
  Facts) still deferred

---

## Code Quality & Simplification Pass

Nine targeted simplifications and optimizations applied across state slices, hooks, and utilities:

1. **Deduplication:** Removed `getDailyFoodLogs as getDailyFoodLogsForRefresh` alias in `coreSlice.ts`
   (single canonical name used everywhere)
2. **Date arithmetic helper:** Extracted `shiftISODate(date, n)` utility in `types/index.ts` to
   eliminate duplicated date logic in 4 call sites (`DateKicker`, `foodSlice`, and future usage)
3. **Functional regression fix:** `activitySlice.ts` now uses `get().selectedDate` instead of
   hardcoded `todayISO()` in 3 places (activity logs now update on past-date navigation, matching
   water/step behavior)
4. **Type safety:** Replaced raw `as ISODate` cast with `ISODate()` constructor in `dbService.ts`
   (validates before branding)
5. **Import consolidation:** `useReducedMotion` now imported from canonical `@/lib/a11y` in
   `StreakCard.tsx` instead of directly from `motion/react`
6. **State simplification:** `dismissedInsights` changed from `Set<string>` to `string[]` in
   `Dashboard.tsx` (eliminates verbose `new Set([...prev, id])` spread pattern)
7. **DB efficiency:** Eliminated redundant `syncQueue.count()` query in `useSyncService.ts` by
   passing count via callback from already-fetched entries array
8. **Unused import cleanup:** Removed unused `ISODate` import from `foodSlice.ts`
9. **Readability:** `cn()` used in `StreakCard.tsx` for conditional `className` (replaces
   `[...].join(" ")` array-join pattern; consistent with `cn()` usage everywhere else)

**Test delta:** No new tests required; all existing tests pass with changes (2025 tests).

---

## Test Coverage

**UX uplift complete (current - pending commit):**

- 113 test files, 2117 tests
- TC1-TC4 all complete; all 13 UX uplift themes complete
- Coverage: Statements 90.78%, Branches 81.34%, Functions 90.4%, Lines 92.52%
- New test files added since first wave (107 files / 2025 tests):
  - `useWeeklyHarvestTrigger.test.ts`
  - `CommandPalette.test.tsx`
  - `ContextualCoachmark.test.tsx`
  - `ActivityTracker.test.tsx` (TC1)
  - `RecurringMeals.test.tsx` (TC2)
  - `OnboardingBanner.test.tsx` (TC4)
  - `haptics.test.ts`
- Test files updated in progress visualization: `EditorialChartCard.test.tsx`,
  `useProgressData.test.ts`, `WeeklySummary.test.tsx`, `useWeeklySummary.test.ts`

**AI photo logging (planned - B5):**

- Target: maintain >90% coverage (statements, functions, lines) with new test files
- New test files expected for AI photo logging backend client and `PhotoFoodLogger` integration

---

## Upgrade Notes

When AI photo logging backend ships:

- Add `S3_BUCKET`, `S3_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` to
  `apps/backend/.env`
- Add `ANTHROPIC_API_KEY` or `OPENAI_API_KEY` to `apps/backend/.env`
- Run `docker compose down && docker compose up -d` to pick up new env vars
- Flyway will auto-apply `V5__photos.sql` on next backend start
- Dexie v20 migration is automatic on next frontend load (additive: `photos` table added, no data
  removed)

---

**Questions?** Open an issue on GitHub
